version: '3.9'

services:
  pharmacist-db-server:
    image: postgres:latest
    container_name: pharmacist-db-server
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 15s
      timeout: 10s
      retries: 5
    volumes:
      - pharmacist-db-server-volume:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: brytech
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ${DATABASE_NAME}
    ports:
      - '5602:5432'
    networks:
      - pharmacist-network

  pharmacist-db-admin:
    image: dpage/pgadmin4
    container_name: pharmacist-db-admin
    volumes:
      - pharmacist-db-admin-volume:/var/lib/pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: ${DATABASE_ADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${DATABASE_ADMIN_PASSWORD}
    ports:
      - '5603:80'
    depends_on:
      pharmacist-db-server:
        condition: service_healthy
    networks:
      - pharmacist-network

volumes:
  pharmacist-db-server-volume:
    driver: local
  pharmacist-db-admin-volume:
    driver: local

networks:
  pharmacist-network:
    driver: bridge

